name: Send Slack notification on pull request
on:
  workflow_call:
    secrets:
      UCP_WEBHOOK:
        required: true
  pull_request:
    types: [ opened, reopened ]

jobs:
  slack_PR_notification:
    runs-on: [ self-hosted, Linux, X64, gcp, small ]
    if: ${{ github.base_ref == 'main'}}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - name: Get user's e-mail
        id: user_email
        run: |
          echo "email=$(git log -n 1 --pretty=format:%ae)" >> $GITHUB_ENV
      - name: Notify Slack on pull requests
        if: ${{ contains(env.email, '@users.noreply') || env.email == '' }}
        run: |
          echo "here"
      - name: Find Slack user
        if: ${{ !contains(env.email, '@users.noreply')}}
        id: find-slack-user
        uses: scribd/find-slack-user-action@v1
        with:
          email: ${{env.email}}
          slack-token: ${{ secrets.SLACK_API_TOKEN }}
      - name: Notify Slack on pull requests
        if: ${{ !contains(env.email, '@users.noreply') || env.email == '' }}
        run: |
          echo "${{ steps.find-slack-user.outputs.username }}"