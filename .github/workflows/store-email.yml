name: Push Workflow
on:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: [ self-hosted, macOS, X64  ]

    steps:

      - name: Use parsed email
        run: |
          echo "here: ${{ github.event.pusher.email }}"